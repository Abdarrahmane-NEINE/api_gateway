def translate_request(request_data: dict) -> dict:
    # Convert client requestion response desired format for the service.
    return request_data

def translate_response(response_data: dict) -> dict:
    # Convert backend response to desired format for the client.
    return response_data
